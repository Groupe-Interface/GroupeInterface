<div class="x_panel">
    <div class="x_content">


        <div class="col-md-12 col-sm-12  form-group">
{{ form_start(formcour) }}
            {{ form_widget(formcour.nomCours,{'attr': {'class': 'form-control w-100 mt-2 mb-2','placeholder': 'Nom Cours'}})}}
            {{ form_widget(formcour.supportCours) }}
            {{ form_widget(formcour.idClasse,{'attr': {'class': 'form-control w-100 mt-2 mb-2','placeholder': 'Classe'}})}}


            <div style="width: 100%; display:flex; justify-content: flex-end">
                <button class="btn btn-primary btn-sm shadow-sm mt-2 mb-2">
                    <i class="fa fa-plus-square-o"></i>
                    {{ button_label|default('Save') }}</button>
            </div>
            {{ form_end(formcour) }}
        </div>
    </div>
</div>
