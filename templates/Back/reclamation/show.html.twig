{% extends 'Back/baseBack.html.twig' %}

{% block content%}
    <h1>Reclamation</h1>
    <div style="width: 100%; display:flex; justify-content: flex-end">
        {{ include('Back/reclamation/_delete_form.html.twig') }}
    </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
            <tr>
                <th>TitleReclamation</th>
                <td>{{ reclamation.titleReclamation }}</td>
            </tr>
            <tr>
                <th>DescriptionReclamation</th>
                <td>{{ reclamation.descriptionReclamation }}</td>
            </tr>
            <tr>
                <th>DateReclamation</th>
                <td>{{ reclamation.dateReclamation ? reclamation.dateReclamation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
    {% if is_granted ('ROLE_ADMIN') %}
    <a href="{{ path("admin_reclamation_index") }}" class="d-none d-sm-inline-block btn btn-sm shadow-sm">
        <button type="button" class="btn btn-dark">
            <i class="fa fa-arrow-circle-left">  </i>  retour
        </button>
    </a>
    {% endif %}
    {% if is_granted ('ROLE_ETUDIANT') %}
        <a href="{{ path("etudiant_reclamation_index") }}" class="d-none d-sm-inline-block btn btn-sm shadow-sm">
            <button type="button" class="btn btn-dark">
                <i class="fa fa-arrow-circle-left">  </i>  retour
            </button>
        </a>
    {% endif %}
    {% if is_granted ('ROLE_ENSEIGNANT') %}
        <a href="{{ path("enseignant_reclamation_index") }}" class="d-none d-sm-inline-block btn btn-sm shadow-sm">
            <button type="button" class="btn btn-dark">
                <i class="fa fa-arrow-circle-left">  </i>  retour
            </button>
        </a>
    {% endif %}
{% endblock %}
