{% extends 'Back/baseBack.html.twig' %}

{% block listeClasse %}
    <ul class="nav child_menu">
        {% for classe in classes %}
            <li><a href="{{ path('enseignant_classe_show', {'id': classe.id}) }}">{{ classe.niveauClasse }}--{{ classe.idSpecialite }}--{{ classe.numClasse }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}

    {% block listeMatiere %}
        <ul class="nav child_menu">

            {% for matiere in matieres %}
                {% if matiere.idSpecialite == classe.idSpecialite%}
                    <li><a href="{{ path('etudiant_matiere_show', {'id': matiere.id}) }}">{{ matiere.nomMatiere }}</a></li>

                {% endif %}
            {% endfor %}

        </ul>
    {% endblock %}

{% block content %}
    {% if is_granted ('ROLE_ADMIN') %}
        <h1>La liste des réclamations </h1>
        <div class="right_col" role="main">
            <div class="">
                <div class="page-title">
                    <table id="datatable" class="table table-striped table-bordered" style="width:100%" >
                        <thead>
                        <tr>
                            <th>Id</th>
                            <th>TitleReclamation</th>
                            <th>DescriptionReclamation</th>
                            <th>DateReclamation</th>
                            <th>Nom et prenom </th>
                            <th>actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for reclamation in reclamations %}
                            <tr>
                                <td>{{ reclamation.id }}</td>
                                <td>{{ reclamation.titleReclamation }}</td>
                                <td>{{ reclamation.descriptionReclamation }}</td>
                                <td>{{ reclamation.dateReclamation ? reclamation.dateReclamation|date('Y-m-d') : '' }}</td>

                                {% for etudiant in etudiants %}
                                    {% if reclamation.users.email == etudiant.emailEtudiant %}
                                        <td>{{ etudiant.nomEtudiant }} {{ etudiant.prenomEtudiant}}</td>
                                    {% endif%}
                                {% endfor %}

                                {% for enseignant in enseignants %}
                                    {% if reclamation.users.email == enseignant.emailEnseignant %}
                                        <td>{{ enseignant.nomEnseignant }} {{ enseignant.prenomEnseignant}}</td>
                                    {% endif%}
                                {% endfor %}
                                <td>
                                    <a href="{{ path('admin_reclamation_show', {'id': reclamation.id}) }}"><i class="fa fa-eye"></i> </a>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="5">no records found</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    {% endif %}
    {% if is_granted ('ROLE_ETUDIANT') %}
        <h1>mes reclamation</h1>
        <div class="right_col" role="main">
            <div class="">
                <div class="page-title">
                    <table id="datatable" class="table table-striped table-bordered" style="width:100%" >
                        <thead>
                        <tr>
                            <th>Id</th>
                            <th>TitleReclamation</th>
                            <th>DescriptionReclamation</th>
                            <th>DateReclamation</th>
                            <th>Nom et prenom </th>
                            <th>actions</th>
                        </tr>
                        </thead>
                        <tbody>
                            {% for reclamation in reclamations %}
                            {% if reclamation.users.email == user.email %}

                                <tr>
                                    <td>{{ reclamation.id }}</td>
                                    <td>{{ reclamation.titleReclamation }}</td>
                                    <td>{{ reclamation.descriptionReclamation }}</td>
                                    <td>{{ reclamation.dateReclamation ? reclamation.dateReclamation|date('Y-m-d') : '' }}</td>

                                    {% for etudiant in etudiants %}
                                        {% if reclamation.users.email == etudiant.emailEtudiant %}
                                            <td>{{ etudiant.nomEtudiant }} {{ etudiant.prenomEtudiant}}</td>
                                        {% endif%}
                                    {% endfor %}
                                    <td>
                                        <a href="{{ path('etudiant_reclamation_show', {'id': reclamation.id}) }}"><i class="fa fa-eye"></i> </a>
                                        <a href="{{ path('etudiant_reclamation_edit', {'id': reclamation.id}) }}"><i class="fa fa-edit"></i> </a>

                                    </td>
                                </tr>


                            {% endif %}

                        {% endfor %}

                        </tbody>
                    </table>
                    <div style="width: 100%; display:flex; justify-content: flex-end">
                        <a href="{{ path('etudiant_reclamation_new') }}"><button type="button" class="btn  btn-success btn-sm shadow-sm mt-2 mb-2"><i class="fa fa-plus-square-o">   </i>  Ajouter</button></a>
                    </div>
                </div>
            </div>
        </div>
    {% endif %}
    {% if is_granted ('ROLE_ENSEIGNANT') %}
        <h1>mes reclamation</h1>
        <div class="right_col" role="main">
            <div class="">
                <div class="page-title">
                    <table id="datatable" class="table table-striped table-bordered" style="width:100%" >
                        <thead>
                        <tr>
                            <th>Id</th>
                            <th>TitleReclamation</th>
                            <th>DescriptionReclamation</th>
                            <th>DateReclamation</th>
                            <th>Nom et prenom </th>
                            <th>actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for reclamation in reclamations %}
                            {% if reclamation.users.email == user.email %}

                                <tr>
                                    <td>{{ reclamation.id }}</td>
                                    <td>{{ reclamation.titleReclamation }}</td>
                                    <td>{{ reclamation.descriptionReclamation }}</td>
                                    <td>{{ reclamation.dateReclamation ? reclamation.dateReclamation|date('Y-m-d') : '' }}</td>

                                    {% for enseignant in enseignants %}
                                        {% if reclamation.users.email == enseignant.emailEnseignant %}
                                            <td>{{ enseignant.nomEnseignant }} {{ enseignant.prenomEnseignant}}</td>
                                        {% endif%}
                                    {% endfor %}
                                    <td>
                                        <a href="{{ path('enseignant_reclamation_show', {'id': reclamation.id}) }}"><i class="fa fa-eye"></i> </a>
                                        <a href="{{ path('enseignant_reclamation_edit', {'id': reclamation.id}) }}"><i class="fa fa-edit"></i> </a>

                                    </td>
                                </tr>


                            {% endif %}

                        {% endfor %}

                        </tbody>
                    </table>
                    <div style="width: 100%; display:flex; justify-content: flex-end">
                        <a href="{{ path('enseignant_reclamation_new') }}"><button type="button" class="btn  btn-success btn-sm shadow-sm mt-2 mb-2"><i class="fa fa-plus-square-o">   </i>  Ajouter</button></a>
                    </div>
                </div>
            </div>
        </div>
    {% endif %}
    <script>
        $(document).ready( function () {
            $('#table_id').DataTable();
        } );</script>
{% endblock %}

