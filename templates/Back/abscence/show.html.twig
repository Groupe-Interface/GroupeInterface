<!DOCTYPE html>

<title>Abscence</title>

{% block body %}
    <h1>Abscence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abscence.id }}</td>
            </tr>
            <tr>
                <th>Justifee</th>
                <td>{{ abscence.justifee ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>CommentaireAbscence</th>
                <td>{{ abscence.commentaireAbscence }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('abscence_index') }}">back to list</a>

    <a href="{{ path('abscence_edit', {'id': abscence.id}) }}">edit</a>

    {{ include('abscence/_delete_form.html.twig') }}
{% endblock %}
