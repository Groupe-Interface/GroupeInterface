{% extends 'Back/baseBack.html.twig' %}

{% block content %}
    <h1>Specialite index</h1>
<div class="right_col" role="main">
    <div class="">
        <div class="page-title">
            <table id="datatable" class="table table-striped table-bordered" style="width:100%" >
        <thead>
            <tr>
                <th>Id</th>
                <th>NomSpecialite</th>
                <th>DureeSpecialite</th>
                <th>NbrSemestre</th>
                <th>Departement</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for specialite in specialites %}
            <tr>
                <td>{{ specialite.id }}</td>
                <td>{{ specialite.nomSpecialite }}</td>
                <td>{{ specialite.dureeSpecialite }}</td>
                <td>{{ specialite.nbrSemestre }}</td>
                <td>{{ specialite.departement }}</td>
                <td>
                    <a href="{{ path('specialite_show', {'id': specialite.id}) }}"><i class="fa fa-eye"></i> </a>
                    <a href="{{ path('specialite_edit', {'id': specialite.id}) }}"><i class="fa fa-edit"></i></a>
                    <a href="{{ path('specialite_delete', {'id': specialite.id}) }}"><i class="fa fa-trash-o"></i></a>

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
            <div style="width: 100%; display:flex; justify-content: flex-end">
                <a href="{{ path('specialite_new') }}"><button type="button" class="btn  btn-success btn-sm shadow-sm mt-2 mb-2"><i class="fa fa-plus-square-o">   </i>  Ajouter</button></a>
            </div>

        </div>
    </div>
</div>
{% endblock %}
